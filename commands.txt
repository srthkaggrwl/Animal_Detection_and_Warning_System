source venv/bin/activate
streamlit run streamlit_app.py runserver 0.0.0.0:8501
sudo shutdown -r now
pip show streamlit
pip install paho-mqtt streamlit
pip install paho-mqtt

for running the process in backend
nohup streamlit run streamlit_app.py runserver 0.0.0.0:8501 &

for checking the process information
ps -ax | grep streamlit 

for stopping the process
kill -9 67967
 - -9 is for abrupt killing
 - 67967 is the process id that can we seen with the above command ps -ax | grep streamlit 